#include<bits/stdc++.h>
using namespace std;

// pair is like dictionary in python
// store key value pair

int main() {
    // pair<int,char> x;
    // x.first = 1;
    // x.second = 'm';

    // int arr[] = {10, 16, 7, 14, 5, 3, 2, 9};
    // vector<pair<int, int>> x;

    // for (int i=0;i<n;i++){
    //     x.push_back(make_pair(arr[i], i));
    // }

    // for (int i=0;i<x.size();i++){
    //     cout << x[i].first << " " << x[i].second << endl;   
    // }

    // pair<int, int> p;
    // p = {12, 22};
    // cout << p.first << " " << p.second << endl;

    return 0;
}