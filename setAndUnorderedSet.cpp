#include<bits/stdc++.h>
using namespace std;

// set uses red black tree 
// set stores unique element

// unordered set uses hashing
// best used for insert delete search 


int main(){

    unordered_set<int> s;

    // unordered_set functions = insert = insert
    // s.insert(10);
    // s.insert(20);
    // s.insert(30);
    // s.insert(40);
    // s.insert(50);

    // unordered_set functions = delete = erase
    // s.erase(20);
    // s.erase(s.begin(), s.end());
    
    // unordered_set functions = search = find
    // auto it = s.find(20); // return 20
    
    // if(s.find(20) != s.end()){
    //     cout << "True" << endl;
    // }else{
    //     cout << "False" << endl;
    // }


    // int x = s.size(); // returns size
    
    // for(auto x : s){
    //     cout << x << " ";
    // }


    return 0;
}