#include<bits/stdc++.h>
using namespace std; 
int main() {
    string s1 = "most common statement in the world";

    // transform(s1.begin(), s1.end(), s1.begin(), ::toupper);
    // cout << s1 <<endl;
    // Reverse order
    // sort(s1.begin(), s1.end(), greater<int>());
    // cout << s1 << endl;

    // Max char in string 
    // temp array 0 indexes
    // char s2[26];
    // for (int i=0;i<26;i++){
    //     s2[i] = 0;
    // }

    // for (int i=0;i<s1.size();i++){
    //     s2[s1[i] - 'a']++;        
    // }

    // char ans = 'a';
    // int maxF = 0; 
    // for (int i=0;i<26;i++){
    //     if (s2[i] > maxF) {
    //         maxF = s2[i];
    //         ans = i + 'a';
    //     }
    // }

    // cout << maxF << " " << ans << endl;

    

    return 0;
}