#include<iostream>
#include<vector>
using namespace std;

int main() {
	// Initialization of vector
	// vector<data type> nameOfVector;
	vector<int> x;
	// vector<int> x1 (10, 0); // 10 elements with 0 value

	// Adding element at the end of the vector
	// x.push_back(1);
	
	// Removing element from the end of the vector
	// x.pop_back();

	// Size of the vector
	// x.size();

	// Swaping two vectors
	// swap(x, x1);

	// Sorting of vector
	// sort(x.begin(), x.end());


	// vector input
	// for(auto &x : arr){
    //     cin >> x;
    // }


	return 0;
}